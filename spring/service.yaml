apiVersion: v1
kind: Service
metadata:
  name: service-spring
spec:
  selector:
    app: spring-aws
  ports:
    - name: "service-spring-aws"
      port: 8081
      targetPort: 8081
      nodePort: 30000
  type: NodePort
